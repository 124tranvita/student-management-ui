export enum ValidationMesg {
  required = "Required",
  minLength = "Value must be more than",
  maxLength = "Value must be less than",
  notNumber = "Value must be number",
}

export enum Typography {
  textTitle = "text-title",
  textBase = "text-base",
  textMuted = "text-muted",
  textName = "text-name",
  sizeNormal = "normal",
  sizeSmall = "small",
  sizeLarge = "large",
}
